#!/bin/sh

if [ "${TINYVM_HOME}" != "" ]; 
then
  echo Warning: TINYVM_HOME is no longer needed.
fi

export TINYVM_HOME="`dirname $0`/.."
export TINYVMOPTIONS="-Dtinyvm.write.order=BE ${TINYVMOPTIONS}"

${TINYVM_HOME}/bin/aux-tvmld $@
